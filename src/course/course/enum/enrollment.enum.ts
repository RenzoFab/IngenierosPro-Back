export enum EnrollmentState {
  Active = 1,
  Inactive = 0,
}

export enum EnrollmentPackageType {
  Promotion = 1,
  Specialization = 0,
}

export enum EnrollmentAcces {
  HAS_ACCESS = 1,
  NO_ACCESS = 0,
}
